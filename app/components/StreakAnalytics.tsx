interface StreakAnalytics {
  totalDays: number;
  successRate: number;
  longestStreak: number;
  currentStreak: number;
  weeklyAverage: number;
  monthlyTrend: number[];
}

const calculateAnalytics = (streak: Streak): StreakAnalytics => {
  // Implementation
}; 